import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>css gallery</title>
      </Head>
      岡崎流依
    </div>
  );
};

export default Home;
